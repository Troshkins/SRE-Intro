name: CI

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push gateway image
        run: | 
          docker build -t ghcr.io/troshkins/quickticket-gateway:${{ github.sha }} ./app/gateway
          docker push ghcr.io/troshkins/quickticket-gateway:${{ github.sha }}

      - name: Build and push events image
        run: |
          docker build -t ghcr.io/troshkins/quickticket-events:${{ github.sha }} ./app/events
          docker push ghcr.io/troshkins/quickticket-events:${{ github.sha }}
        
      - name: Build and push payments image
        run: |
          docker build -t ghcr.io/troshkins/quickticket-payments:${{ github.sha }} ./app/payments
          docker push ghcr.io/troshkins/quickticket-payments:${{ github.sha }}
